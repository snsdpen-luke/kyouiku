# つなぐ工房　デザイン方向 3案

実装ではない。**色と骨格を決めるための絵。**
同じ画面（課題2・運転モード）を三通りに描き分けてある。

ラダー工房と同じ顔にすると生徒が科目を区別しない、というユーザー判断により、
**ラダー工房の要素（ベージュ地・橙のランプ・緑の通電・金属パネル・丸ボタン）は使っていない。**

| ファイル | 方向 | 色 | 骨格 |
|---|---|---|---|
| `Main.dc.html` | **A 計測器** | 深い紺 `#080d12` ＋ シアン `#22d3ee` | 等幅・角ばった・発光。暗い画面 |
| `DirectionB.dc.html` | **B 製図ノート** | 白 ＋ 黒 ＋ 朱 `#c1121f` の3色だけ | 明朝体・細い線・余白・罫線 |
| `DirectionC.dc.html` | **C 積み木** | 白地 ＋ 紫 `#6d28d9`／青緑 `#0d9488`／桃 `#db2777` | 太い枠・大きな字・影つきブロック |

`canvas.json` に各案の強みと弱みを付箋で入れてある。

**一つ選んでから、残り4枚（コンセプト／編集モード／半加算器／ヒントと判定）を
その方向で描く。** 3案×5枚は作らない。

`tsunagu-kobo-ui.html` は上をまとめた生成物。手で編集しない。
