# ラダー工房 — 開発引き継ぎ書

最終更新: 2026-09-13 ／ 現行版: `版 2026-09-13d` ／ 主ファイル: `ラダー工房.html`（単一HTML・945行）

---

## 0. これは何か

工業高校「電子計測制御31」の授業で使う、**三菱系PLCのラダー図エディタ＋シミュレータ**。
生徒がChromebookのブラウザで開き、ラダー図を組んで仮想の機械を動かす。

- **単一HTMLファイル。** ビルド不要、依存ライブラリなし、通信なし、インストール不要
- 生徒は1人1台のChromebookで使う（**タップ操作前提**、ドラッグに依存しない）
- Google Classroomにファイルを置いて配付する運用

### 教育上の位置づけ（設計判断の背景）

実習室の実機では、配線とソフト操作に時間を取られて**論理を考える時間が残らない**。
このアプリで自己保持・インターロック・タイマを先に体得させ、実機実習の中身を濃くするのが目的。
**実機を置き換えるものではない。**

---

## 1. まず知るべき「壊してはいけない」性質

ここを崩すと教材として嘘になる。変更時は必ず §6 の検証を回すこと。

### 1-1. 実行意味論は実機に合わせてある（測定済み）

| 挙動 | 実装上の根拠 |
|---|---|
| 上の行の結果が**同一スキャン**で下の行に伝わる | `scanOnce` が `prog` を上から順に評価し、コイル書き込みが即座に `p.Y`/`p.M` を更新 |
| 行の順を逆にすると**1スキャン遅れる** | 同上の自然な帰結。**意図的にこうしてある** |
| 二重コイルは**後の行が勝つ** | 上書き実装の帰結。実機と一致 |
| タイマのT接点が**同一スキャン**で下の行に効く | `TON` 処理でその場で `p.T[n]` を更新 |
| スキャン中に入力Xが変化しない | `scanOnce` は `p.X` を一切書き換えない（**この不変条件を破らないこと**） |
| 並列ブロックへの逆流が起きない | 入れ子構造で表現しているため構造的に不可能 |

> **注意**: `scanOnce` の中で `p.X` に書き込むコードを足すと、入力の一貫性が壊れる。
> 機械からのセンサ入力は `MACHINES[].sense()` で**スキャンの外**から入れること。

### 1-2. スキャン周期は意図的に遅い

`const SCAN=0.05;`（50ms）。実機は1〜10ms。
**目で電気の流れを追えるようにするための教育的判断**であって、バグではない。
論理の正誤には影響しない（比率は保たれる）。

---

## 2. ファイル構造（`ラダー工房.html` 行番号は現行版）

```
  1-187  <style>        CSS（デバイス盤 / ラダー / 操作 / ポップアップ）
188-223  PLC心臓部       SCAN, newPLC, bit, evalSer, scanOnce, C/PAR/R ヘルパ
226      DEVS           使用可能デバイス一覧（編集ポップの選択肢）
229-351  MACHINES       仮想機械4種（door / cross / vend / elev）
352-495  LESSONS        レッスン定義13件（基本7 + 課題5 + 自由1）
496-499  状態変数        idx, prog, plc, marks, mach, mstate, hintLv, sawAns ...
502-543  画面遷移        tabs, load, nav
544-616  デバイス盤      collectDevs, drawDev, drawDevMachine, drawExtra
617-689  ラダー描画      contHTML, elHTML, serHTML, drawLadder, locate
690-790  編集UI          pop, devPicker, openEl, openCoil, drawHints
791-808  RUN/STOP       start, stop（setInterval 50ms）
809-895  自動判定        runMachine, runProgram, checkBtn ハンドラ
896-945  提出            openSubmit, hintSummary, vals, recText
```

---

## 3. データ構造

### 3-1. ラダープログラム

```js
// 接点
{t:"c", dev:"X0", neg:false}      // neg:true で b接点

// 並列ブロック（入れ子可）
{t:"p", br:[ [要素...], [要素...] ]}

// ラング（1行）
{elems:[要素...], coil:{t:"OUT"|"TON", dev:"Y0", k:30}}
//                      k は TON のみ。K値=0.1秒単位（K30 = 3.0秒）

// プログラム全体
prog = [ラング, ラング, ...]
```

ヘルパ: `C("X0", true)` / `PAR([...],[...])` / `R(elems, coil)`

**評価規則**: ラング = `elems` を直列AND。並列ブロックは各枝のORで、
枝は「そのブロックに入ってきた電力」を起点に評価する（`evalSer(br, p, before)`）。

### 3-2. PLC状態

```js
{X:{}, Y:{}, M:{}, T:{},   // キーは番号のみ。"X0" → p.X["0"]
 Tacc:{},                   // タイマ積算（秒）
 scans:0, t:0}
```

### 3-3. レッスン定義

```js
{
 id:8, kind:"t"|"q"|"free", name:"課題1 自動ドア",
 machine:"door",            // 任意。MACHINES のキー。絵を出す
 closed:true,               // 任意。閉ループ判定（elev のみ）
 title, body, hint,         // 表示文（body/hintはHTML可）
 start:[...],               // 開始時のラダー（未完成であること）
 ref:[...],                 // 正解ラダー。判定の基準
 hints:["段1","段2","段3"],  // 3段階ヒント
 halfway:[...],             // ヒント3で挿入される「半分できた回路」
 tl:[[scan,"X0",1],...],    // 入力タイムライン（closed でない場合）
 len:260,                   // 判定スキャン数
 watch:["Y0","Y1"],         // 比較する出力
 press:[{d:"X0",a:30}],     // closed の場合のボタン操作（4スキャン押す）
 after:"正解後の解説"
}
```

### 3-4. 仮想機械

```js
{
 name, io:[{dev:"X0", role:"btn"|"sens"|"out", lab:"1階 呼びボタン"}],
 spd,                          // 任意（elev のかご速度）
 init(){ return 状態; },
 sense(st,p){ ... },           // スキャン前。センサ → p.X に書く
 act(st,p,spd){ ... },         // スキャン後。p.Y → 機械の状態を更新
 draw(st,p){ return SVG文字列; }
}
```

- `role:"btn"` … 生徒がタップする（押している間ON）
- `role:"sens"` … **タップ不可**。`sense()` が機械の状態から決める（閉ループの肝）
- `role:"out"` … 出力ランプ表示

---

## 4. 判定のしくみ（ここが設計の要）

**構造ではなく動作で判定する。** 生徒が違う組み方をしても、正しく動けば正解。

1. 生徒の `prog` と `ref` を、**同じ入力**で同じスキャン数だけ走らせる
2. `watch` に挙げた出力を毎スキャン比較
3. 最初に食い違ったスキャンを特定し、「Y0が4.4秒の時点でONですが、正しくはOFFです」と提示

- `closed:true` → `runMachine()`（機械を挟んだ閉ループ。かごの**位置**も比較）
- それ以外 → `runProgram()`（`tl` のタイムラインで入力を与える）
- 短絡検出: `runMachine` が `st.short` を立てると専用メッセージ

### 課題を追加・変更するときの必須確認

過去に**3回、判定に穴が開いた**。いずれもタイムライン設計のミス。

| 過去の失敗 | 内容 |
|---|---|
| タイマ課題 | X0を押す時間が3秒に足りず、**タイマが一度も完成しなかった**。誤答と区別できず |
| 自動ドア | 観測時間が短くドアが一度も閉じず、**タイマなしの誤答でも合格**した |
| 正逆転・エレベーター | 反対ボタンを押すのが停止後で、**インターロックが一度も試されなかった** |

→ **新しい課題を足したら、必ず「典型的な誤答」を書いて、それが落ちることを確認する。**
　 §6 のテストにケースを追加すること。

---

## 5. 現在の内容

### レッスン（13件）

| # | 種別 | 名前 | 学ぶこと |
|---|---|---|---|
| 1 | 基本 | 接点とコイル | RUNして観察するだけ（**初期状態で正解になる。意図的**） |
| 2 | 基本 | 直列（AND） | 接点を横に並べる |
| 3 | 基本 | 並列（OR） | 枝分かれ |
| 4 | 基本 | b接点（NOT） | 斜線の接点 |
| 5 | 基本 | 自己保持 | 自分の出力を条件に戻す |
| 6 | 基本 | タイマ | TON / K値 |
| 7 | 基本 | 内部リレー M | 外に出ないリレー |
| 8 | 課題 | 自動ドア（door） | 自己保持＋オフ遅延 |
| 9 | 課題 | 踏切（cross） | 自己保持＋オン遅延＋直列 |
| 10 | 課題 | 自動販売機（vend） | 自己保持＋自分の出力で解除 |
| 11 | 課題 | 正逆転 | インターロック |
| 12 | 課題 | エレベーター（elev, **閉ループ**） | 自己保持＋リミット停止＋インターロック |
| 13 | 自由 | 自由に組む | 制限なし |

### ヒント（3段階＋お手本）

1. 考え方だけ　2. 具体的な構成　3. **`halfway` の回路が実際に挿入される**

**`halfway` は必ず「未完成」でなければならない**（5課題すべてで確認済み）。
最後の一手は自分で組ませる設計。別枠で「お手本を見る」があり、`sawAns` に記録される。

### 記録・提出

`hintLv`（課題ごとの使用段数）、`sawAns`、`checks`（確かめた回数）、`done` を記録し、
コピー / .txt ダウンロードで提出。**Googleフォーム連携は未実装**（別アプリ「制御エンジニアチャレンジ」には実装済み。そちらのコードが参考になる）。

---

## 6. 検証のやり方（**変更したら必ず回す**）

ブラウザ実機がないので **jsdom** でヘッドレス検証している。
**`regression.js` を同梱してある。これが現時点の全リグレッション（40項目・全合格）。**

```bash
npm install jsdom
node regression.js                    # 既定は ラダー工房.html
node regression.js ラダー工房v2.html   # 対象を指定（2026-09-13 追加）
```

失敗があれば exit code 1。

中身は4部構成:
- **【A】実行意味論** — 上→下の伝播、1スキャン遅れ、二重コイル後勝ち、入力の一貫性
- **【B】全レッスン** — `ref`＝正解 / `start`＝未完成 / `halfway`＝未完成
- **【C】典型的な誤答** — 課題ごとに6ケース。これが落ちることを保証
- **【D】UIの基本動作** — 接点タップ、並列追加、機械描画、センサ枠、ヒント段階

**課題を足したら【C】にその課題の誤答ケースを必ず追加すること。**

自分でテストを書く場合の雛形:

テストの雛形:

```js
const {JSDOM,VirtualConsole}=require("jsdom");
const fs=require("fs");
const vc=new VirtualConsole(); vc.on("jsdomError",e=>{});
const dom=new JSDOM(fs.readFileSync("ラダー工房.html","utf8"),
 {runScripts:"dangerously",pretendToBeVisual:true,virtualConsole:vc});
const w=dom.window,d=w.document;
w.scrollTo=()=>{}; w.alert=()=>{}; w.confirm=()=>true;   // jsdom未実装APIを潰す
w.HTMLElement.prototype.scrollIntoView=function(){};      // これも未実装。忘れると errs に出る
const $=id=>d.getElementById(id);
const errs=[]; w.onerror=(m,s,l,c,e)=>{errs.push((e&&e.stack||m).split("\n")[0]);return true;};

// 内部状態は w.eval で直接触れる
w.eval(`idx=8; load(); prog=clone(L().ref);`);
$("checkBtn").click();
console.log($("verdict").className.includes("ok"));
```

### 最低限のリグレッション（全レッスンに対して）

1. `ref` を入れて「正解」と判定されるか
2. `start` のままで「未完成」と判定されるか（レッスン1だけは例外）
3. `halfway` が「未完成」と判定されるか
4. 典型的な誤答が落ちるか（課題ごとに個別に用意）
5. `errs` が空か

### 構文チェック

```bash
node -e 'const fs=require("fs");const h=fs.readFileSync("ラダー工房.html","utf8");
 fs.writeFileSync("/tmp/p.js",h.split("<script>")[1].split("</script>")[0]);' \
 && node --check /tmp/p.js
```

---

## 7. 既知の制限（意図的に実装していないもの）

**命令セット**: `OUT` と `TON` のみ。以下は未実装。

- `SET` / `RST`
- `PLS` / `PLF`（立ち上がり・立ち下がり検出）
- カウンタ `C`
- `MOV`・比較・四則演算
- `MC` / `MCR`、ステップラダー（SFC）

**その他**

- タイマは100ms系のみ（10ms系・積算保持形なし）
- 回路の形に制限あり: **直列＋並列ブロックの入れ子のみ**。複雑に交差する分岐や、1行に複数コイルは組めない
- END処理・WDT（ウォッチドッグ）なし
- 二重コイルの**警告が出ない**（挙動自体は実機どおり後勝ち）
- ニモニック表示なし、GX Worksファイルの入出力なし
- デバイス数がごく少ない（`DEVS` で定義: X0-X3, Y0-Y2, M0-M1, T0-T1）
- チャタリング・入力フィルタなし

### 教材上の注意点（次の担当も引き継ぐべき）

自販機課題の商品排出 `Y0` は**1スキャンだけON**になる。
50msなので画面では見えるが、実機（1ms）ではリレーが反応しない可能性がある。
本来は `SET`/`PLS` で保持する。**「アプリだから見えている」現象**として生徒に伝える必要がある。

---

## 8. 次にやると効きそうなこと（優先度順・ユーザー未確定）

ユーザー（つよし先生）は「全部の機能は要らない、実機で最初に使う命令から」という方針。
**どれを足すかは本人に確認してから着手すること。**

1. **`PLS`（立ち上がり検出）** — 自販機の「1本だけ」が本来の書き方で書ける。教材効果が大きい
2. **`SET` / `RST`** — 自己保持を2行で書く実務的な方法
3. **カウンタ `C`** — 「5個数えたら」系の課題が作れる
4. **二重コイルの警告** — 実機で怒られる前に気づける
5. **ニモニック表示** — ラダーの横に `LD X0 / OR Y0 / ANI X1 / OUT Y0` を出す
6. **仮想機械の追加** — 信号機、コンベア搬送、パレタイザなど。§3-4 の形に沿えば足せる
7. **Googleフォーム提出** — 「制御エンジニアチャレンジ」に実装済みのコードを移植

---

## 9. 関連する既存教材（同じ生徒向け・作成済み）

| ファイル | 内容 |
|---|---|
| `制御エンジニアチャレンジ.html` | フィードバック制御（P/I/PID）のゲーム。30分制限＋エンドレス調整。**Googleフォーム提出を実装済み** |
| `シーケンス設計工房.html` | ラダー工房の前身。穴埋め式でドア／踏切／自販機を設計 |
| `電子計測制御31_振り返り調べ学習シート.docx` | 主体的に学習に取り組む態度の評価資料 |
| `進路シミュレーション.html` | **進路学習（高2）向け。生活費から必要な手取りを逆算し、求人票の読み方を学ぶ。同じリポジトリにあるが科目は別。引き継ぎ書は `HANDOFF-進路シミュレーション.md`** |

同梱ファイル:

| ファイル | 内容 |
|---|---|
| `ラダー工房v2.html` | **本体。直すのはここだけ**（§12） |
| `index.html` | GitHub Pages が配るもの。`build.py` の**生成物**。手で編集しない |
| `ラダー工房.html` | 引き継ぎ前の現行版。無傷で温存 |
| `regression.js` | リグレッションテスト（jsdom・85項目） |
| `build.py` | 本体から配布物を作る |
| `README.md` | 配布URL・別PCでの始め方・更新手順 |
| `HANDOFF.md` | この引き継ぎ書 |

授業は2コマ構成を想定。1コマ目にチャレンジ、2コマ目にラダー工房、という流れ。

---

## 10. ユーザーとのやりとりで確定している方針

- **PLCの流儀は三菱電機**（X/Y/M/T、K値、GX Works系）。実習の実機に合わせている
- **生徒が1人1台で触る**（先生のデモ用ではない）
- **基本操作を学んでから課題に挑戦**する構成
- **クリアできなくても次へ進める**こと（詰まって止まると先の面白い所に触れられない）
- **デバイス盤には、回路で実際に使っているデバイスだけを表示**する（`collectDevs()`）
- ゲーム内の文章は**普通の説明文**（キャラクター口調にしない）
- 画面に**版の表示**を出す（`版 2026-09-13d`）。Chromebookで古いファイルを開きがちなため

---

## 11. 変更時のチェックリスト

- [ ] `scanOnce` の中で `p.X` を書き換えていないか（入力の一貫性）
- [ ] 実行意味論7項目が保たれているか（§1-1）
- [ ] 全レッスンで `ref`＝正解 / `start`＝未完成 / `halfway`＝未完成 になるか
- [ ] 課題ごとの典型的な誤答が落ちるか
- [ ] `node --check` が通るか
- [ ] jsdom で `errs` が空か
- [ ] 版の表示を更新したか

---

## 12. `ラダー工房v2.html`（UI刷新版・2026-09-13 着手／版 2026-09-13f）

**目的**: 実機の制御盤らしい見た目にし、道具としての質を上げる。機能追加ではない。

**配色は三菱FX系の実機に寄せたベージュ／白**（2026-09-13f で暗色から転換）。
ユーザー指定。実習室の実機がベージュの筐体であることに合わせている。

| 要素 | 色 | 根拠 |
|---|---|---|
| 筐体・盤面 | ベージュ `#e9e4d7` 系 | 実機の筐体色 |
| 紙・ラダー図の地 | 白 `#ffffff` ＋薄い方眼 | GX Works の編集画面 |
| **入出力LED（X / Y）** | **橙 `#f59e0b`** | FXシリーズの入出力LEDが橙 |
| **ラダーの通電** | **緑 `#15a34a`** | LEDと別物なので色が混ざらない |
| 選択中の要素 | 青 `#2563eb` | |
| RUN / ERR ランプ | 緑 / 赤 | |

> 入出力LEDとラダー通電で**色を分けているのは意図的**。実機でも両者は別の表示。
> どちらかに揃えると、「盤のランプ」と「電気の流れ」が混ざって読めなくなる。

**出力 Y は φ22 パイロットランプのレンズを模してある**（2026-09-13k、`.ylamp:before`）。
消灯時はくすんだ乳白のドーム、点灯時は中心が白く光って外側へ橙のにじみが出る。
センサ（`.sens:before`）は**四角いレンズ**にしてランプと形で区別する（リミットスイッチのつもり）。
`radial-gradient` と二重の `box-shadow`（内側の陰＋外側のにじみ）で作っている。
**寸法を変えるときは `min-height` も一緒に見ること。** レンズだけ大きくすると行が詰まる。

### 方針（重要）

**ロジックは凍結してある。** `scanOnce` / 判定エンジン / `LESSONS` 13件 / `MACHINES` 4種は
`ラダー工房.html` と同一。書き換えたのは **CSS と描画層だけ**。

表示のためだけに足した例外が4か所ある。いずれも計算には関与しない。

| 場所 | 足したもの |
|---|---|
| `start()` / `stop()` | RUN LED の点灯・消灯 |
| `flash()` | 判定が `bad` のとき ERR LED を点灯 |
| `load()` | ERR LED を消す／課題文の折りたたみを開く |
| `drawExtra()` | ステータス帯（MODE/SCAN/TIME/CYCLE/VER）の描画 |

### 変えたもの

- **フェイスプレート**: CPUユニット風のヘッダ。POWER / RUN / ERR の3灯
- **ステータス帯**: `MODE STOP ｜ SCAN 0 ｜ TIME 0.00s ｜ CYCLE 50ms ｜ VER …` を等幅で常時表示
- **2カラム**: 左＝課題文・機械の絵・ラダー／右＝制御盤（`position:sticky`）。1040px 以下で1カラム
- **制御盤**: 入力Xは押し込まれる物理ボタン、出力Yは丸LED、センサは斜線ハッチの枠。すべて44px以上
- **ラダー**: 方眼の背景、母線を6pxに、**右母線を追加**、接点とコイルを一回り大きく
- **課題文の折りたたみ**: 1366×768 に「絵＋制御盤＋ラダー2行＋操作」を収めるため。
  レッスンを移ると必ず開く（課題文を読ませるため）
- **タブ**: 「基本」「課題」の2行に群分け。提出は課題行の末尾に破線で置く

### 機械の絵（`MACHINES[].draw()`）だけは色を書き換えた

**`sense()` / `act()` / `init()` は一切触っていない。`draw()` が返す SVG の色だけを置換した。**
暗い背景用の色がハードコードされていたため、白い紙の上で見えなくなったからである。

```
#0b1119 背景 → #ffffff     #2b3a4a 枠線 → #a89e89    #8fa3b8 文字 → #6d6555
#26313d 消灯 → #cdc5b3     #f5b544 点灯 → #f59e0b    #6ee7a8 ON  → #15a34a
#4f9dea 動く物体 → #3b82f6  #f5645a 赤  → #d93b2b     #38485a 床・縁 → #8b8271
```

**機械を足すときは、この明るい配色で描くこと**（§3-4 の形は変わっていない）。

### 直した不備（旧版から持ち越していたもの）

**「お手本に戻す」は名前が嘘だった。** 実際の処理は `prog=clone(L().start)` で、
お手本（`ref`）ではなく**開始時の未完成回路**に戻していた。
生徒は「正解が出る」と思って押し、組んだ回路を失う。`flash()` の案内文も
「自分の回路に戻し」と書いてあったが、その時点で自分の回路はもう消えている。**二重に嘘。**

直した内容:

| | 旧 | v2 |
|---|---|---|
| ラベル | 「お手本に戻す」 | **「最初からやり直す」** |
| 置き場所 | 移動の行（navrow） | 編集の行（edrow）。移動の行は「← 前へ／次へ →」だけになった |
| 確認 | なし | 回路が変わっているときだけ `confirm` |
| 機械の状態 | **リセットされない**（かごの位置が残る） | `mstate=mach.init()` でリセット |
| RUN中 | 止まらない | `stop()` する |
| 案内文 | 「自分の回路に戻し」 | 「はじめの回路に戻ります」 |

`regression.js` 【D】に4件のテストを足した（旧版では `#resetProg` が無いので自動で飛ばす）。
**「お手本」という語はもう `ansBtn`（お手本を見る）にしか使っていない。**
UIの語を足すときは、**その語が指すものが1つだけか**を必ず確かめること。

### 「リレーが見えない」への手当て（2026-09-13h）

ユーザーから「自己保持はリレー接点が要るのに、リレーがない」との指摘。
実装は正しかった（⑤の `ref` は `Y0` の a接点で自己保持）が、**画面のどこにも「リレー」の語が出ていなかった。**

| 直した所 | 内容 |
|---|---|
| 接点を選ぶポップ | `X0 X1 … T1` のベタ並びをやめ、**「入力 X」「出力リレー Y」「内部リレー M」「タイマ T」の見出し**で区切った |
| 制御盤 | **「つかえるデバイス」欄を新設**（`drawTools()`）。種別ごとに一覧を出し、「Y と M はリレーなので、その接点を条件に使えます」と注記 |
| ⑤自己保持の本文 | 「出力 Y0 は**リレー**です。コイルであると同時に**接点としても使えます**」を追加。ヒントも「出力リレー Y0 の a接点」に |

「つかえるデバイス」は**答えを漏らさない**ように、`ref` ではなく `DEVS`（＝常に同じ顔ぶれ）を出している。
機械つきレッスンのときだけ、X と Y を機械の `io` に絞る。

### ⚠ レッスンの順序を変えてはいけない（「内部リレー M」を前に出す案について）

**「リレーを先に教えるべきだから ⑦内部リレー M を ⑤自己保持 の前に移す」は、やってはいけない。**
一度検討して却下した。理由は2つ、どちらも構造的なもの。

**理由1: ⑦の正解回路が自己保持を使っている。**

```js
ref: [R([PAR([C("X0")],[C("M0")]),C("X1",true)],{t:"OUT",dev:"M0"}),   // ← M0 の自己保持
      R([C("M0")],{t:"OUT",dev:"Y0"})]
```

前に出すと、まだ習っていない自己保持を要求することになる。前提が壊れる。

**理由2: 「M を中継に使うだけ」のレッスンは、この判定方式では作れない。**

§4 のとおり判定は**構造ではなく動作**で見る。
`X0 →( M0 )` / `M0 →( Y0 )` という中継だけの回路は、`X0 →( Y0 )` の直結と**動作が完全に一致する**。
つまり「M を使わない誤答」を落とせない。**動作判定では M の使用を強制できない。**

⑦が「M0 を自己保持する」形になっているのは、**そうしないと M を使う必然が生まれないから**である。
自己保持があって初めて「押しっぱなしにできない → 記憶が要る → M が要る」が成立する。

→ **⑤自己保持 → ⑦内部リレー M の順序は、判定方式から導かれた必然。** 入れ替えるなら判定方式ごと変える話になる。
　 リレーの概念は、⑤の本文で「Y はリレーであり接点としても使える」と教えることで先に導入してある。

### お手本は「別枠」で出す（2026-09-13i）

**旧版の「お手本を見る」は `prog` を `ref` で上書きしていた。**
自分の回路が消えるので、**見比べることができない**。「見た後で何が違ったのか分からない」状態だった。

v2 では別枠 `#refbox` に読み取り専用で描く。

- `ladderHTML()` を `drawLadder()` から切り出した。`drawRefLadder()` が `prog` を一時的に差し替えて
  同じテンプレートで `#refladder` に描き、**必ず元に戻す**
- `#refladder` にはイベントを付けない（`.ro` クラスで `addbtn` を隠し、hover も殺す）
- お手本の下に**その回路の説明（`l.after`）**を出す。12レッスン全部に `after` がある（`自由に組む` のみ無いが、
  そこは `ansBtn` 自体が非表示）
- 「この回路を入れて試す」を押せば、従来どおり `prog` に入れて RUN で動かせる（確認あり）
- `sawAns` への記録は従来どおり。確認ダイアログは**初回だけ**出す
- レッスンを移ると自動で閉じる

`regression.js` 【D】に8件追加。中でも重要なのは **「お手本を見ても自分の回路が消えない」**。
ここが壊れると、この機能の意味が丸ごと無くなる。

> `drawRefLadder()` は `prog` / `marks` / `sel` を**一時的に書き換えて元に戻す**。
> 例外が飛ぶと戻らないので、この関数の中に判定や副作用を足さないこと。

### 「盤のランプ Y0」と「接点の Y0」が同じものだと説明する（2026-09-13j）

ユーザーから「出力の Y0 と出力リレーの Y0 が同じものに見えて分かりにくい。実機もこうなのか」との指摘。

**答えは「実機もそう。同じもので合っている」。** 三菱の `Y` は**出力リレー**で、
コイルが1つ、接点は何個でも使える。コイルが入れば外の端子がONになり、
**同時に**プログラム内で接点として使える。PLCが有接点リレー回路の置きかえとして作られた名残である。

**正しいのに画面が説明していなかった**ので、説明を足した。

- 制御盤の見出しを `OUTPUT Y ／ 出力` → **`OUTPUT Y ／ 出力リレー（外の機械が動く）`** に統一
- 「つかえるデバイス」欄に **「リレーってなに？」** を置き、`relayHelp()` が図つきで開く
  - 図: 1つのコイル `( Y0 )` から、右は**端子→ランプ**へ、下は**接点（何個でも）**へ分かれる
  - 「同じ1つのリレー」と明示
  - X（接点だけ）／ Y（コイルも接点も）／ M（外に端子がない）の対比表

> **生徒がここで必ずつまずく。** 「出力なのに条件に使えるのはなぜか」は自己保持の入口そのもの。
> 用語を足すときは `X:入力リレー / Y:出力リレー / M:内部リレー` で統一すること。

### 横長タブレット（Chromebook）向けの版組み（2026-09-13n）

授業で使うのは**横長のChromebookタブレット**。実効ビューポートは 1366×690 前後（ブラウザのバーを引いた値）。
**縦が足りない**のが唯一にして最大の制約。

| 変えた所 | 前 | 今 |
|---|---|---|
| 機械の絵の位置 | 左カラム（課題文の下）。**ラダーを画面外へ押し出していた** | **右カラムの一番上**。ラダーが常に画面の上に来る |
| 右カラム幅 | 376px | **480px**（絵が読める大きさになる） |
| 2カラムのしきい値 | 1040px | **820px**（下の「⚠」を読むこと） |
| 右カラム | 伸びっぱなし | `max-height:calc(100vh - 20px)` ＋ 内部スクロール。絵とボタンが常に上に残る |
| 機械の絵の文字 | 10.5〜14 | **12.5〜16**（右カラムで 0.86 倍に縮むため） |

さらに `@media(min-width:821px) and (max-height:820px)` で、**縦が狭い横長画面のときだけ**
ヘッダ・パネル・ラダー行の余白を詰める。デスクトップの見た目は変えない。

> ### ⚠ 1カラムに落とすしきい値を上げてはいけない（2026-09-13t・ユーザーから強い指摘あり）
>
> **入力ボタンと出力ランプは、常に画面の右にあること。** これは動かせない要求。
>
> 一度 480px 固定＋しきい値 1140px にしたところ、**ユーザーの画面幅がそれに届かず1カラムに落ち、
> ボタンが下に回った。** 「横長のUIにしてと言ったのに」と指摘を受けた。**同じ失敗を繰り返さないこと。**
>
> 今の作り:
> ```css
> .cols{grid-template-columns:minmax(0,1fr) minmax(320px,460px)}   /* 右は伸び縮みする */
> @media(max-width:820px){ .cols{grid-template-columns:minmax(0,1fr)} }
> ```
> **右カラムを固定幅に戻さないこと。** 幅が足りないときは右カラムが縮んで耐える作りにしてある。
> 1カラムに落ちるのは、2カラムがどうにも成立しない 820px 未満だけ。
> 900px / 1100px / 1366px で2カラムになることを実機幅で確認済み。

**結果**: 1366×690 に「課題文＋ラダー3行＋機械の絵＋制御盤」が全部入る（踏切で確認）。
縦持ち 768×1024 でも1カラムで破綻しない。

> エレベーターの絵は、**シャフトを右に50ずらした**（`x=150→200` ほか）。
> 文字を大きくしたら `▲ 上昇モータ ON` がシャフトに重なったため。
> **機械の絵の文字サイズを触るときは、必ず4機械すべてを目で見ること。** 座標は直書きで、重なりは実行するまで分からない。

### ついでに直した表示バグ

`stop()` は RUN ランプを消すがステータス帯を描き直していなかったため、
**STOP した後も `MODE RUN` のまま**残っていた。`runBtn` のハンドラで `drawDev()` を呼ぶようにした。
`regression.js` に3件追加（RUN/STOP で MODE が追従するか、ランプが消えるか）。

### 編集モードと運転モードを分けた（2026-09-13p）

ユーザーから「**回路を作る画面と、作った回路を確かめる画面**を分かりやすく分けてほしい」との要望。

**実機の作法そのものを持ち込んだ。** GX Works の書込み／モニタ、PLC本体の RUN/STOP スイッチに当たる。

| | 編集モード（STOP） | 運転モード（RUN） |
|---|---|---|
| `PROGRAM 回路を組む` | **青**のバッジと枠。「接点をタップして組みかえられます」 | 灰。「**運転中は回路を変えられません（実機と同じ）**」 |
| `MONITOR 動かして確かめる` | 灰。「STOP中。入力を押すと1スキャンだけ動きます」 | **緑**のバッジと枠。「入力を押すと動きます」 |
| 接点／コイルのタップ | 開く | **開かない**（`openEl` / `openCoil` の冒頭で `return`） |
| 「＋接点」 | 出る | **消える**（CSS） |
| 行を追加／削除／最初からやり直す | 押せる | **`disabled`** |
| ラダーの地色 | 白 | うっすら緑 |

`document.body.dataset.mode` が `"edit"` / `"run"` を持ち、CSS がそれを見て顔つきを変える。
切り替えは `setMode()` 一箇所（`start()` / `stop()` から呼ぶ）。

> **STOP中に入力Xをタップすると1スキャンだけ走る**機能は残してある（`redrawLive`）。
> 組みながら通電を確かめられるので、編集モードでも「確認」はできる。これは実機にはない親切。

**入力と出力を横に並べた**（`.io2`、`1fr 22px 1fr` のグリッド）。間に `→` を置いて
「入力 → 出力」の流れを見せる。実機の端子台の並びに近い。560px 以下では縦積みに落ちる。

`regression.js` に8件追加。**中でも「運転中に回路が変わらない」が要**。

### 接点の追加・削除を作り直した（2026-09-13q）

ユーザーから「接点を追加したり削除したりするのが分かりにくい」との指摘。
調べたところ、**操作が遠回りで、しかも2つ目の嘘ラベルがあった。**

**⚠ `delRung`「選んだ行を削除」も嘘だった。** 中身は `prog.pop()` ＝ **最後の行を消す**。
選択とは何の関係もない。（1つ目は「お手本に戻す」。**同じ種類の事故が2回起きている。**）

| | 旧 | 新 |
|---|---|---|
| 接点を足す | 接点をタップ→ポップ→「右に直列で追加」。または行の右端の「＋接点」で**末尾に**追加 | **図の上、接点と接点のあいだに出る「＋」**。押した場所に入る |
| 接点を消す | 接点をタップ→ポップ→削除 | 同じ（ポップに残す） |
| 行を消す | 「選んだ行を削除」（実体は最後の行） | **行ごとの「この行を消す」**。押した行が消える（確認あり） |

廃止したもの: `.addbtn`（＋接点）、`#delRung`、ポップの「右に直列で追加」。
**どれも「＋」に統合できるか、嘘だったもの。**

実装の要は `insHTML(path,i)` と `locate()` の組み合わせ。
`data-ins="r0.1.b0.2"` は「そのパスの配列の 2 番目に挿入する」を意味する。
`locate()` は**末尾位置（`i === arr.length`）でも `{arr, i}` を正しく返す**ので、
末尾への挿入も同じ仕組みで書ける。**ここは壊さないこと。**

見た目は意図的に控えめ（薄いグレー、枠なし）。**接点より目立ってはいけない。**
ただし当たり判定は `min-height:44px` を確保してある。
運転中（`body[data-mode="run"]`）とお手本表示（`.ro`）では出さない。

`regression.js` に6件追加。**要は「消えたのは押した行。最後の行ではない」**——嘘の再発防止。

> **教訓（2回目）**: ボタンのラベルと実装がずれる事故がこのアプリで2回起きている。
> UIの文言を書いたら、**その動きを検査するテストを必ず【D】に足すこと。**

### UIの洗練（2026-09-13r）

#### 1. ヒント3段目が黙って回路を上書きしていた（実害）

「お手本を見る」は別枠にしたのに、**ヒント3（`halfway` の挿入）だけ上書きのまま残っていた**。
確認も出ず、生徒が組んだものが消える。`start` と違うときだけ確認を出すようにした
（「最初からやり直す」と同じ方針）。キャンセルしたら `hintLv` も増やさない。

#### 2. タイマをゲージにした

`T0:OFF(1.2/3.0s)` というテキストを `T0 [████░░░░] 1.2 / 3.0s OFF` のゲージに。
**タイマは基本⑥・課題1・課題2で使う中心概念**で、溜まっていく様子が見えるかどうかで理解が変わる。
`.tmr` / `.tbar` / `.tbar i`（幅が % で伸びる）。

#### 3. 入力Xを「切替スイッチ」と「押しボタン」に分けた

**同じ見た目なのに動きが違っていた。**

| | 動き | 見た目 |
|---|---|---|
| 機械なしのレッスン（`drawDev`） | タップでON/OFFが切りかわる | **トグルスイッチの絵**（`.swknob` がスライドする）＋「入 ON / 切 OFF」。押し込まれない |
| 機械つきの課題（`drawDevMachine`） | 押している間だけON | 従来の**押し込まれるボタン** |

見出し（`#xlab`）も `（タップで切りかえ）` / `（押している間だけON）` と描き分ける。
実機でも押しボタンと切替スイッチは別の部品。**動きが違うものを同じ顔にしない。**

#### 4. ステップ実行 —— 死んでいた `scanIdx` を生かした

`scanIdx` と `.rung.scanning` は**一度も使われないデッドコード**だった。これを実装に変えた。

**`scanOnce` に第4引数 `onRow` を足した。** ロジック凍結の唯一の例外。

```js
function scanOnce(p,prog,marks,onRow){
  for(let ri=0;ri<prog.length;ri++){
    ...（評価・コイル書き込みは一行も変えていない）...
    if(onRow) onRow(ri);     // ← 覗き窓。呼ぶだけ
  }
  p.scans++; p.t+=SCAN;
}
```

- ループを `for...of` から index ループに変えただけで、**評価順・計算は同一**
- 既存の呼び出しは `onRow` 省略＝従来どおり（後方互換）
- `stepScan()` が行ごとの `marks` を控え、430ms ごとに再生して行を上から下へ光らせる
- **§1-1 の「上の行の結果が同じスキャンで下の行に伝わる」を、目で見せられる**
- 再生中は `stepping=true`。編集もRUNも止まり、`stop()` で中断できる

> **この改造の安全性は【A】の2件で保証してある。**
> `onRow を付けても計算結果は1ビットも変わらない`（5スキャン分の Y/M/T/Tacc/scans/t を比較）
> `onRow は行を上から順に1回ずつ呼ぶ`
> **`scanOnce` を今後さわるときは、必ずこの2件が通ることを確かめること。**

（`CYCLE 50ms` が実機と違う件の注記は、ユーザーが選ばなかったので入れていない。§1-2 参照。）

### 並列の描き方を実機に合わせた（2026-09-13s）

ユーザーが実機（GX Works）のラダー図を示して「ちゃんとこう並列になってほしい」と指摘。
**現状は並列を「箱」として描いていて、母線のすぐ右にもう1本縦線が立ち、二重線になっていた。**

実機では、**行の先頭にある並列は母線そのものが分岐点**になる。左に余分な縦線は引かない。

| | 旧 | 新 |
|---|---|---|
| 行頭の並列の左 | `.par:before` で縦線（母線と二重） | **引かない**（`.par.first:before{display:none}`） |
| 母線と枝のあいだ | `wire` と `＋`（挿入ボタン）が挟まる | **何も挟まない**。枝が母線に直に付く |
| 分岐・合流の縦線 | `top:14px;bottom:14px`（半端な位置） | `top:28px;bottom:28px` ＝ **上の枝の中心から下の枝の中心まで** |

`elHTML(el,path,first)` の第3引数と `serHTML` の `headPar` 判定で「行の先頭の並列か」を伝える。
**データ構造（`{t:"p",br:[...]}`）は一切変えていない。描画だけの変更。**

> 行頭の並列の「前」に直列接点を挿入する `＋` は出なくなった。
> 母線に接して見せることを優先した判断。**レッスン13件のどれもこの操作を必要としない**（自己保持・
> 踏切・エレベーターはすべて「並列が先頭」で、その前に足す場面がない）。

**実機と違うまま残す所（2026-09-13 決定済み。蒸し返さないこと）**

ユーザーの指示は「**枝分かれの仕方だけ実機を参考にする**」。線の引き方だけを合わせ、他は揃えない。

| 実機との差 | 判断 | 理由 |
|---|---|---|
| **行番号** | **今のまま**（コイル1つ分の回路に1番号） | 実機は並列の枝ごとに番号を振るが、そこまでは合わせない。「この回路はひとまとまり」が見える方が教材に向く。**行ごとの削除ボタンもこの単位で動いている** |
| **END 行** | **出さない** | §7 のとおり命令として未実装。**実装していない命令を絵だけ出すのは嘘になる** |

### ⚠⚠ ブラウザの `confirm` / `alert` を使ってはいけない（2026-09-13v・実害が出た）

**ユーザーから「行が消えない」との報告。原因はこれだった。**

```
サンドボックス内での実測:  confirm("…") → ダイアログを出さず即 false を返す
                          alert("…")   → 何も表示しない
```

Classroom のプレビューや埋め込み表示など、**モーダルを禁じられた環境では `confirm` が常に false**。
`if(!confirm(...)) return;` という書き方をしていた機能が、**全部まるごと死んでいた。**

- この行を消す／最初からやり直す／お手本を見る（初回）／この回路を入れて試す／ヒント3

**しかもテストが通っていた。** `regression.js` が `w.confirm=()=>true` と差し替えていたため、
**実環境と正反対の条件で検査していた。**

対処:

1. **アプリ自前の `ask(msg, onYes, yesLabel)` と `tell(msg)` に全置換。**
   既存の `pop()` を使うので見た目も揃う。`confirm`/`alert` の呼び出しは**ゼロ**
2. `regression.js` を**実環境どおり**に変えた
   ```js
   const usedModal=[];
   w.alert  =m=>{ usedModal.push("alert: "+m); };
   w.confirm=m=>{ usedModal.push("confirm: "+m); return false; };   // ← false！
   const yes=()=>{ const b=$("okBtn"); if(b) b.click(); };          // 自前の「はい」を押す
   ```
3. 【E】として **「ブラウザの confirm / alert に頼っていない」** を検査

> **教訓（3回目の同種事故）**:
> 1回目「お手本に戻す」、2回目「選んだ行を削除」はラベルと実装のズレ。
> 3回目のこれは**テストの前提が実環境と違っていた**という、より悪い型。
> **テストの環境設定（`w.xxx=...` の行）は、実環境で本当にそうなのか確かめてから書くこと。**
> 楽な方（`confirm=()=>true`）に倒すと、バグを見逃す側に倒れる。

実ブラウザで最終確認済み: 行番号タップ → 「この行を消す」 → 自前の確認 → 「消す」で
**3行 → 2行。消えたのは押した行**。

### 守った制約

- `regression.js` 【D】が依存する id を維持した
  （`#ladder .el` / `#pop` / `#pPar` / `#xrow .sens` / `#machbox` / `#mach` / `#hintbox`）。
  **UIをさらに触るときも、この id の契約は壊さないこと。**壊すと検証網が死ぬ
- 40項目すべて合格。`node regression.js ラダー工房v2.html` で確認できる

### 未確定

- **ファイル名が仮**（`v2`）。配付名は決まっていない
- どちらを授業で使うかは未決。`ラダー工房.html` は無傷で残してある

---

## 13. 配布（2026-09-14）

git 管理を始めた。**<https://github.com/snsdpen-luke/kyouiku>**（public）。

### 生徒に配るURL（これが本番）

**<https://snsdpen-luke.github.io/kyouiku/>**

Classroom にはこのリンクを貼る。**ファイルは配らない。**
§10 の「Chromebookで古いファイルを開きがち」問題が、構造的に消える。

未ログインのブラウザで実測済み: 開く／RUN でスキャンが進む／入力スイッチと出力ランプが動く／
通電が流れる／運転モードに切り替わる／提出の記録が出る。**アカウントは要らない。**

### 直したときの手順

```bash
node regression.js ラダー工房v2.html    # 1. 検証（必ず通す）
python3 build.py                        # 2. index.html を作り直す ← 忘れると反映されない
git add -A && git commit -m "…" && git push     # 3. これで生徒の画面が変わる
```

本体の `const VER="…"` も更新すること。画面右上に出る。

### claude.ai の Artifact 版

<https://claude.ai/code/artifact/d2439011-5e59-4e41-afd6-bc7372803205>

`python3 build.py <出力先>` で断片を作って publish する。`capabilities: {downloads: true}` 付き。

> **⚠ Artifact には制約がある。**
> 「Anyone with the link（誰でも開ける）」と「Shared version: Latest（最新版を自動で配る）」は
> **同時に選べない**。生徒に配る設定にすると版が固定され、**更新のたびに共有メニューで
> 手動で新しい版に切り替える**ことになる。
> → **配布は GitHub Pages を主にする。** Artifact は予備。

### サンドボックスで死ぬもの（Artifact 版で踏んだ実害）

| 使えないもの | 代わり |
|---|---|
| `confirm` / `alert` | 自前の `ask()` / `tell()`（§ 上の「⚠⚠」を読むこと） |
| `<a download>` / script からの保存 | `claude.use("downloads")`。両対応のコードが `btnDl` に入っている |
| `navigator.clipboard`（環境による） | 記録を `#recOut` のテキスト枠に必ず出し、選んでコピーできるようにしてある |

**新しい機能がブラウザのAPIに頼るときは、まずサンドボックスで動くか確かめること。**
