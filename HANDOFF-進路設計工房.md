# 進路設計工房 — 開発引き継ぎ書

最終更新: 2026-09-18 ／ 現行版: `版 2026-09-18b` ／ 主ファイル: `進路設計工房.html`（単一HTML・1,526行）

---

## 0. これは何か

高校2年の**進路学習**で使う教材。生徒が自分の生活費を組み立て、そこから
**必要な手取り → 必要な額面 → 求人票の合格ライン**を逆算し、
**ハローワークが高校に出す「求人票（高卒）」と同じ様式**の求人を読む。

- 単一HTMLファイル。ビルド不要、依存ライブラリなし、**通信なし**、インストール不要
- Chromebook 1人1台・タップ操作前提（操作部品はすべて44px以上）
- 提出機能は**持たない**（ユーザーと確定済み。授業内で考えさせるための道具）

### 教育上の位置づけ（設計判断の背景）

進路指導で「初任給18万円」と聞いても、生徒はそれが多いのか少ないのか判断できない。
**比べる基準が自分の中に無い**からだ。だから順序を逆にしてある。

1. 先に「自分がどう暮らしたいか」を決めさせ、**必要額を自分で作らせる**
2. そのあとで求人票を見せる。**基準ができているので、金額が判断できる**

求人票を先に見せない。これは意図的。金額から入ると、いちばん大きい数字に目が吸われて、
年間休日も固定残業代も離職者数も読まなくなる。

---

## 1. まず知るべき「壊してはいけない」性質

### 1-1. 金額は生徒に打たせない（**この教材の核**）

高2に「食費はいくら？」と空欄を見せても、正しい数字は出てこない。
**「こんな暮らし」を選ばせて、金額はこちらが渡す。**

```js
{v:32000, nm:"自炊と外食が半々", d:"平日は自炊、週末は外食。いちばん多い形"}
```

金額・暮らしの名前・**その暮らしの具体**（`d`）の3点セットで1つの選択肢。
`d` が抜けると、ただの数字の羅列に戻って教材として死ぬ。
選択肢を足すときは必ず3点そろえること。リグレッション【B】が見張っている。

「自分で入れる」（`S.pick[id] === -1`）は逃げ道として残してある。
**家の請求書を見て打ち込む生徒のためのもので、既定にはしない。**

### 1-2. 計算は概算。ただし**構造**は正しくなければならない

1円まで当てることは目的ではない。次の3つを体で分からせることが目的で、
ここが崩れると教材として嘘になる。

| 伝えたいこと | 実装上の根拠 |
|---|---|
| 額面と手取りは違う（約8割） | `tedori()` が社会保険料・所得税・住民税を順に引く |
| **2年目に住民税が来て手取りが減る** | `tedori(g,year2)` の `year2` で住民税を切り替え、STEP2で必ず並べて見せる |
| 月給だけでは求人を比べられない | `calcJob()` が年間休日と残業から**1時間あたり**を出す |

### 1-3. ブラウザの `confirm` / `alert` を使ってはいけない

Classroom のプレビュー等、モーダルを禁じられた環境では `confirm` が**常に false を返す**
（ラダー工房で実害が出た。`HANDOFF.md` §12 の「⚠⚠」を読むこと）。
このアプリは自前の `ask(msg,fn,label)` / `tell(msg)` だけを使う。
リグレッション【G】が、`confirm`/`alert` を呼んだら落とすようにしてある。

### 1-4. `localStorage` は失敗する前提で書く

`save()` / `load()` は必ず `try` で包む。埋め込み表示では例外が飛ぶ環境がある。
**保存できなくてもアプリは動く**こと。テストで検査している。

### 1-5. 生徒の選択を勝手に書き換えない

`S.edited[id]` が立っている費目は、暮らし方を変えても既定で上書きしない。
`whatIf()`（「もし実家だったら」の試算）も、計算後に `S.v` と `S.pick` を必ず元へ戻す。
ここを壊すと、生徒が20分かけて選んだものが一瞬で消える。

---

## 2. ファイル構造

```
     1-253  <style>       CSS（ヘッダ / 目次 / 費目カード / 求人票 / クイズ / 比較）
   255-290  <body>        骨組みだけ。中身はすべてJSが描く
   296-354  計算モデル     RATE, SHAHO, kyuyoKoujo, kisoKoujo, tedori, gakumen
   356-502  くらしと費目   LIVES（3種）, GRPS（6群）, ITEMS（22費目・各3〜4の選択肢）
   504-538  状態と道具     S, shown, optsOf, defIdx, applyDefaults, sumGrp, outMonth
   540-641  アドバイス     advice() … 選んだ中身に応じて生徒ごとに変わる一言
   643-727  保存・骨組み   save/load, ask/tell, STEPS（5段階）, go, drawTabs, drawSide
   729-775  費目を描く     itemHTML, groupHTML, bindItems
   777-914  段階1〜2      drawKurashi（暮らし方＋毎月＋年に数回）, drawPay
   916-1059  求人票データ   JOBS（3件・本物の様式の全欄）, whatIf, calcJob, verdictOf
  1061-1163  タップ説明     EXPLAIN（28欄。求人データを受け取って求人ごとの数字で語る）
  1165-1260  求人票を描く   kxOpen, kRow, kyujinHTML, bindKyujin
  1262-1450  段階3〜4      quizList, drawKyujin, drawCompare
  1452-1524  段階5・起動    drawSum, 初期化
```

---

## 3. データ構造

### 3-1. 費目 `ITEMS`

```js
{id:"food", grp:"taberu",   // GRPS のどの群に置くか
 g:"m",                     // "m"=毎月 / "y"=年に数回
 per:24,                    // g:"y" のみ。何か月に一度か（車検＝24）
 nm:"食費", tip:"行の下に出る小さい説明（HTML可）",
 only:["alone"],            // この暮らし方のときだけ出す（省略＝全部）
 car:true, nocar:true,      // 車のあり／なしで出し分ける
 opts:[{v:20000,nm:"ほぼ自炊",d:"米を炊いて弁当を作る。…"}, …],   // 共通の選択肢
 optsBy:{alone:[…],home:[…],dorm:[…]},   // 暮らし方ごとに選択肢そのものが変わる場合
 def:{alone:1,home:1,dorm:1}}            // 既定で選ばれる選択肢の番号
```

- `optsOf(it)` が今の暮らし方に応じた選択肢を返す。`optsBy` があればそちら
- 月あたりの額は `monthOf()`。`g:"y"` は `値 ÷ per`
- **表示・合計・判定はすべて `shown()` を通した費目だけ**で計算する

### 3-2. アドバイス `advice(it)`

生徒が**いま選んでいる中身**を見て、その子にだけ意味のある一言を返す。
「みんなに同じ注意書き」は読まれない。読まれない注意書きは無いのと同じ。

```js
case "jzei":{                       // 自動車税の欄で
  const k=val("shaken");            // 車検の選択と合わせて
  const diff=(v-10800)+(k-70000)/2; // 軽自動車との差額を出して見せる
```

- 戻り値は**文字列（HTML可）**。出すものが無ければ空文字
- **他の費目の値を見てよい**（`val("shaken")`）。組み合わせで語れるのが強み
- 追加したら必ずテストを回す。【D】が**全費目 × 全選択肢 × 全暮らし方**で
  例外と `NaN`・`undefined` の混入を検査している（132通り）

### 3-3. 求人票 `JOBS` と タップ説明 `EXPLAIN`

`JOBS` は本物の求人票（高卒）の全欄を持つ。欄の名前・並び・区分は本物に合わせてある
（仕事内容／労働時間／賃金・手当／保険・年金・定年等／選考等／事業所情報／青少年雇用情報）。
**生徒が進路指導室で本物を渡されたとき、同じ場所を指で探せるようにするため。**

`EXPLAIN` は「？」をタップすると開く説明。**求人データを受け取る関数**になっている。

```js
nenkyu:(j)=>"1年は約52週。"+j.nenkyu+" ÷ 52 ＝ <b>週 "+(j.nenkyu/52).toFixed(1)+"日</b>の休み。…"
```

一般論だけの説明は読まれない。**その求人の数字で語ること。**
`kRow(j,"年間休日数",値,"nenkyu")` の第4引数がキー。キーを渡した欄だけがタップできる。

> テスト【E】が、**すべての説明を3件すべての求人に対して呼び出して**壊れないことと、
> 迷子の説明（どの欄にも結びついていない EXPLAIN）が無いことを検査している。

### 3-4. 状態 `S`

```js
{life:"alone"|"home"|"dorm", car:true, step:0,
 v:{費目id:金額}, pick:{費目id:選んだ番号 または -1}, edited:{費目id:1},
 bonus:4.0, quiz:{問番号:選んだ番号}, kx:{"A:nenkyu":true}, seen:{段階:1}}
```

---

## 4. 計算モデルと、**毎年の更新手順**（ここが賞味期限）

税率・保険料率は毎年変わる。**変えるのは `RATE` と2つの控除関数だけ**で済むようにしてある。
授業で使う年度の数字を、必ず確かめて差し替えること。

```js
const RATE={
  kenko:0.0500,    // 健康保険 本人負担（協会けんぽ 約10%の労使折半。都道府県で少し違う）
  nenkin:0.0915,   // 厚生年金 本人負担（18.3%の折半。当面変わらない）
  koyo:0.0055,     // 雇用保険 本人負担（一般の事業）← 毎年度見直される
  shotoku:0.05,    // 所得税率（課税所得195万円以下）
  jumin:0.10,      // 住民税 所得割
  juminFlat:5000,  // 住民税 均等割＋森林環境税
  juminKiso:430000 // 住民税の基礎控除
};
function kyuyoKoujo(a){ … }   // 給与所得控除。190万円まで65万円
function kisoKoujo(shotoku){ } // 所得税の基礎控除。所得に応じて95万〜58万
```

- **確認先**: 協会けんぽ「保険料額表」／ 厚生労働省「雇用保険料率」／ 国税庁「給与所得控除・基礎控除」
- `kisoKoujo` の上乗せ（95万・88万…）は時限の特例を含む。**期限が切れたら58万に一本化**する
- 40歳から介護保険料（約0.8%）が加わるが、高卒18歳の教材なので入れていない
- 数字を直したら `node regression-shinro.js 進路設計工房.html` を回す。
  【A】が手取り率75〜88%・単調増加・逆算の一致を見張っているので、桁を間違えれば落ちる

### 逆算のしくみ

`gakumen(必要手取り, year2)` は**二分探索**。`tedori()` が単調増加なので、
控除の式が複雑になっても逆算の実装を直す必要がない。式を増やしてもここは触らなくてよい。

---

## 5. 画面（5段階）

| 段階 | 中身 | 狙い |
|---|---|---|
| 1 くらしを組み立てる | 暮らし方・車 ＋ 6群22費目を**選択式**で。群ごとに合計、先頭に目次 | 金額を打たせない。**選ぶたびに右の盤が動く**ので、暮らし方と金額が結びつく |
| 2 手取りから額面へ | 控除の内訳・早見表・**求人票の合格ライン** | 額面と手取りの差、**2年目の住民税** |
| 3 求人票を読む | 本物と同じ様式の求人票＋**28欄のタップ説明**＋7問 | 欄の意味を自分でめくらせる。固定残業代・週休二日制・青少年雇用情報 |
| 4 3件を比べる | A/B/C の比較、判定、**届かないとき何を動かすか**の表 | 月給の順位と時給の順位が入れ替わる |
| 5 まとめ | 自分の数字の一覧、求人票で必ず見る欄 | 進路面談で使える形にして持たせる |

**暮らし方・毎月・年に数回は同じ画面**に置いてある（ユーザーと確定）。
別画面にすると、暮らし方を変えたときに金額がどう動くかが見えなくなる。

### 求人3件は「順位が入れ替わる」ように作ってある

| | A ミナミ製作所 | B キタガワ電機 | C ヒガシ運輸 |
|---|---|---|---|
| a＋b | 185,000円 | 171,000円 | **221,000円** |
| 年間休日 | 95日 | **125日** | 96日 |
| 賞与 | 2.0か月 | **4.5か月** | 1.0か月 |
| 固定残業代 | 20時間 30,000円 | なし | 30時間 41,000円 |
| 3年の採用／離職 | 9名 / 2名 | 10名 / 1名 | 15名 / 9名 |
| 1時間あたり | 1,024円 | **1,376円** | 1,099円 |

**月給が一番高いCが、時給では2位に落ちる。** ここが授業の山場。
金額をいじるときは、この関係が崩れていないかを必ず確認すること
（リグレッション【F】が見張っている）。

---

## 6. 検証のやり方（**変更したら必ず回す**）

```bash
npm install jsdom
node regression-shinro.js 進路設計工房.html    # 102項目。すべて合格すること
python3 build.py                                # shinro/index.html を作り直す
```

検査の区分:

- 【A】税と社会保険 … 内訳の合計・単調増加・逆算の一致・手取り率の範囲・控除の境界
- 【B】費目が選択式であること … 選択肢3つ以上・既定が範囲内・暮らし方での入れ替え・月割り
- 【C】画面と操作 … 5段階・全群が1画面にある・目次の飛び先・選択と自由入力・**生徒の選択が守られるか**
- 【D】アドバイス … **全費目×全選択肢×全暮らし方**で壊れないこと、狙った場面で出ること
- 【E】求人票とタップ説明 … 本物の欄名がそろう・28欄が3件すべてで壊れない・迷子の説明がない・開閉
- 【F】求人の計算と判定 … 計算式、通勤手当の上限、**クイズの正解が計算値と一致するか**、判定のしきい値
- 【G】サンドボックス耐性 … confirm/alert 不使用、try で包んだ保存、外部読み込みゼロ

> **クイズの正解は、求人データから計算した値と突き合わせて検査している。**
> 求人の金額を変えても、選択肢の文言と正解が自動で追従する。手で直す必要はない。

### 画面幅の確認（手作業）

Chromium で 390 / 768 / 1366 px の3つを開き、**横スクロールが出ないこと**を見る。
広い表には `class="plain wide"` を付けて、その表だけが横に流れるようにしてある。
`.cols>*{min-width:0}` を消すと、表が列を押し広げて画面から溢れる。消さないこと。

---

## 7. 意図的に実装していないもの

- **提出機能**（ユーザーと確定。授業内で考えさせる道具にとどめる）
- 進学ルート（学費・奨学金の返済）。就職希望が中心という前提で作ってある
- 実在求人の取り込み。求人票は架空。**実物は進路指導室にある**
- 扶養・配偶者控除・生命保険料控除など。18歳独身には効かない
- 手取りの月ごとの変動（賞与月・6月の住民税切り替え）。年額を12で割った平均で見せている

---

## 8. 授業での使い方（案・未実施）

1コマ目: STEP1〜2。自分の必要手取りを出すところまで。
2コマ目: STEP3〜5。求人票を読み、最後に**進路指導室の本物の求人票**を1枚ずつ配って同じ欄をたどる。

- STEP1の選択肢は**教室で手を挙げさせながら進める**と効く。「ほぼ自炊の人？」で笑いが起きる
- STEP4の「暮らし方を変えたら」の表を全員分並べると、同じ求人でも判定が変わるのが一目で分かる
- タップ説明は**先に読ませない**。まず自分で読ませて、詰まった欄だけ開かせる

---

## 9. 次にやると効きそうなこと（優先度順・ユーザー未確定）

1. **求人票に「欠けのある1件」を足す**（固定残業の時間数が空欄、加入保険が2つだけ）。
   「質問すべき求人」を見抜かせる
2. 昇給を10年分伸ばす画面。昇給しても住民税と社会保険料が一緒に増える様子
3. 地域の最低賃金を入力させ、`jikyu` と比べる
4. 選択肢の金額を**地域の実勢に合わせる**（家賃・駐車場は県で大きく違う。先生が1か所で直せるよう
   `ITEMS` の上に定数をまとめてもよい）
