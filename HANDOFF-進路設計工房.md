# 進路設計工房 — 開発引き継ぎ書

最終更新: 2026-09-18 ／ 現行版: `版 2026-09-18a` ／ 主ファイル: `進路設計工房.html`（単一HTML）

---

## 0. これは何か

高校2年の**進路学習**で使う教材。生徒が自分の生活費を積み上げ、そこから
**必要な手取り → 必要な額面 → 求人票の合格ライン**を逆算し、最後に
**ハローワークの求人票（高卒）と同じ欄の並び**で作った架空求人を読む。

- 単一HTMLファイル。ビルド不要、依存ライブラリなし、**通信なし**、インストール不要
- Chromebook 1人1台・タップ操作前提（操作部品はすべて44px以上）
- 提出機能は**持たない**（ユーザーと確定済み。授業内で考えさせるための道具）

### 教育上の位置づけ（設計判断の背景）

進路指導で「初任給18万円」と聞いても、生徒はそれが多いのか少ないのか判断できない。
**比べる基準が自分の中に無い**からだ。だから順序を逆にしてある。

1. 先に「自分がどう暮らしたいか」を決めさせ、**必要額を自分で作らせる**
2. そのあとで求人票を見せる。**基準ができているので、金額が判断できる**

求人票を先に見せない。これは意図的。金額から入ると、いちばん大きい数字に目が吸われて、
年間休日も固定残業代も読まなくなる。

---

## 1. まず知るべき「壊してはいけない」性質

### 1-1. 計算は概算。ただし**構造**は正しくなければならない

1円まで当てることは目的ではない。次の3つを体で分からせることが目的で、
ここが崩れると教材として嘘になる。

| 伝えたいこと | 実装上の根拠 |
|---|---|
| 額面と手取りは違う（約8割） | `tedori()` が社会保険料・所得税・住民税を順に引く |
| **2年目に住民税が来て手取りが減る** | `tedori(g,year2)` の `year2` で住民税を切り替え、STEP4で必ず並べて見せる |
| 月給だけでは求人を比べられない | `calcJob()` が年間休日と残業から**1時間あたり**を出す |

### 1-2. ブラウザの `confirm` / `alert` を使ってはいけない

Classroom のプレビュー等、モーダルを禁じられた環境では `confirm` が**常に false を返す**
（ラダー工房で実害が出た。`HANDOFF.md` §12 の「⚠⚠」を読むこと）。
このアプリは自前の `ask(msg,fn,label)` / `tell(msg)` だけを使う。
リグレッションの【E】が、`confirm`/`alert` を呼んだら落とすようにしてある。

### 1-3. `localStorage` は失敗する前提で書く

`save()` / `load()` は必ず `try` で包む。埋め込み表示では例外が飛ぶ環境がある。
**保存できなくてもアプリは動く**こと。テストで検査している。

### 1-4. 生徒の入力を勝手に書き換えない

`S.edited[id]` が立っている費目は、暮らし方を変えても既定値で上書きしない。
`whatIf()`（「もし実家だったら」の試算）も、計算後に必ず元の状態へ戻す。
ここを壊すと、生徒が20分かけて入れた数字が一瞬で消える。

---

## 2. ファイル構造

```
    1-208  <style>       CSS（ヘッダ / ステータス帯 / 費目の行 / 求人票 / クイズ / 比較カード）
  210-230  <body>        骨組みだけ。中身はすべてJSが描く
  252-305  計算モデル     RATE, SHAHO, kyuyoKoujo, kisoKoujo, tedori, gakumen
  307-342  くらしの費目   LIVES（3種）, ITEMS（22費目）
  344-387  状態と道具     S, shown, sumOf, outMonth, needGross, save/load, ask/tell
  389-455  画面の骨組み   STEPS（7段階）, go, drawTabs, drawStatus, drawSide, render
  457-492  費目の行       rowsHTML, bindRows
  494-647  段階1〜4       drawLife, drawMonth, drawYear, drawPay
  649-774  求人票         JOBS（3件）, whatIf, calcJob, verdictOf, kyujinHTML
  776-950  段階5〜6       quizList, drawKyujin, drawCompare
 952-1012  段階7・起動    drawSum, 初期化
```

---

## 3. データ構造

### 3-1. 費目 `ITEMS`

```js
{id:"rent", g:"m",          // "m"=毎月 / "y"=年に数回
 nm:"家賃・共益費", tip:"行の下に出る小さい説明（HTML可）",
 step:5000,                 // ＋−ボタンの刻み
 per:24,                    // g:"y" のみ。何か月に一度か（車検＝24）
 only:["alone"],            // この暮らし方のときだけ出す（省略＝全部）
 car:true,                  // 車ありのときだけ出す
 nocar:true,                // 車なしのときだけ出す
 def:{alone:55000,home:0,dorm:15000}}   // 暮らし方ごとの初期値
```

月あたりの額は `monthOf()` が出す。`g:"y"` は `値 ÷ per`。
**表示・合計・判定はすべて `shown()` を通した費目だけ**で計算する。

### 3-2. 求人票 `JOBS`

```js
{id:"A", co:"事業所名", num:"求人番号", job:"職種",
 kihon:150000,                                   // 基本給（a）
 teate:[{n:"固定残業手当（時間外20時間分）",v:30000}],  // 定額の手当（b）
 fixedOT:{h:20,v:30000},                         // 固定残業代。無ければ null
 tsukinMax:10000,                                // 通勤手当の上限
 bonus:2.0,                                      // 賞与か月数（基本給にかける）
 nenkyu:95, ot:25, jitsudo:8,                    // 年間休日 / 月平均残業 / 所定労働
 ryouV:15000, ...}                               // 寮費（無ければ0）
```

`calcJob()` が出すもの:

| 値 | 式 | 意図 |
|---|---|---|
| `month` | 基本給＋定額手当 | 求人票の **a＋b**。毎月必ず出る額 |
| `bonusYen` | **基本給**×か月数 | 手当は賞与の土台にならない、を数字で示す |
| `year` | `month`×12＋`bonusYen` | 年収（額面） |
| `hours` | (365−年間休日)×所定＋残業×12 | 年間の実労働時間 |
| `jikyu` | `year`÷`hours` | **1時間あたり**。順位がひっくり返る所 |
| `net2` | 手取り(月)＋通勤手当で戻る分 | 判定に使う「使える額」 |

### 3-3. 状態 `S`

```js
{life:"alone"|"home"|"dorm", car:true, step:0,
 v:{費目id:金額}, edited:{費目id:1}, bonus:4.0,
 quiz:{問番号:選んだ番号}, seen:{段階:1}}
```

---

## 4. 計算モデルと、**毎年の更新手順**（ここが賞味期限）

税率・保険料率は毎年変わる。**変えるのは `RATE` と2つの控除関数だけ**で済むようにしてある。
授業で使う年度の数字を、必ず確かめて差し替えること。

```js
const RATE={
  kenko:0.0500,    // 健康保険 本人負担（協会けんぽ 約10%の労使折半。都道府県で少し違う）
  nenkin:0.0915,   // 厚生年金 本人負担（18.3%の折半。当面変わらない）
  koyo:0.0055,     // 雇用保険 本人負担（一般の事業）← 毎年度見直される
  shotoku:0.05,    // 所得税率（課税所得195万円以下）
  jumin:0.10,      // 住民税 所得割
  juminFlat:5000,  // 住民税 均等割＋森林環境税
  juminKiso:430000 // 住民税の基礎控除
};
function kyuyoKoujo(a){ ... }   // 給与所得控除。190万円まで65万円
function kisoKoujo(shotoku){ }  // 所得税の基礎控除。所得に応じて95万〜58万
```

- **確認先**: 協会けんぽ「保険料額表」／ 厚生労働省「雇用保険料率」／ 国税庁「給与所得控除・基礎控除」
- `kisoKoujo` の上乗せ（95万・88万…）は時限の特例を含む。**期限が切れたら58万に一本化**する
- 40歳から介護保険料（約0.8%）が加わるが、高卒18歳の教材なので入れていない
- 数字を直したら `node regression-shinro.js 進路設計工房.html` を回す。
  【A】が手取り率75〜88%・単調増加・逆算の一致を見張っているので、桁を間違えれば落ちる

### 逆算のしくみ

`gakumen(必要手取り, year2)` は**二分探索**。`tedori()` が単調増加なので、
控除の式が複雑になっても逆算の実装を直す必要がない。式を増やしてもここは触らなくてよい。

---

## 5. 画面（7段階）

| 段階 | 中身 | 狙い |
|---|---|---|
| 1 くらし方を決める | ひとり暮らし／実家／寮 ＋ 車の有無、初期費用 | 前提を自分で選ばせる。**最初の給料は1か月後**に気づかせる |
| 2 毎月かかるお金 | 12費目の増減 | 日用品・散髪・貯金など、見落とす費目を出す |
| 3 年に数回かかるお金 | 車検・自動車税・任意保険・帰省など、月割り | **18〜21歳の任意保険が高い**ことを数字で見せる |
| 4 手取りから額面へ | 控除の内訳・早見表・**求人票の合格ライン** | 額面と手取りの差、**2年目の住民税** |
| 5 求人票を読む | 架空求人A＋7問のクイズ | 固定残業代・前年度実績・年間休日・通勤手当の上限 |
| 6 3件を比べる | A/B/C の比較、判定、**届かないとき何を動かすか**の表 | 月給の順位と時給の順位が入れ替わる |
| 7 まとめ | 自分の数字の一覧、求人票で必ず見る欄 | 進路面談で使える形にして持たせる |

### 求人3件は「順位が入れ替わる」ように作ってある

| | A ミナミ製作所 | B キタガワ電機 | C ヒガシ運輸 |
|---|---|---|---|
| a＋b | 185,000円 | 171,000円 | **221,000円** |
| 年間休日 | 95日 | **125日** | 96日 |
| 賞与 | 2.0か月 | **4.5か月** | 1.0か月 |
| 1時間あたり | 1,024円 | **1,376円** | 1,099円 |

**月給が一番高いCが、時給では2位に落ちる。** ここが授業の山場。
金額をいじるときは、この関係が崩れていないかを必ず確認すること
（リグレッション【D】が見張っている）。

---

## 6. 検証のやり方（**変更したら必ず回す**）

```bash
npm install jsdom
node regression-shinro.js 進路設計工房.html    # 74項目。すべて合格すること
python3 build.py                                # shinro/index.html を作り直す
```

検査の区分:

- 【A】税と社会保険 … 内訳の合計・単調増加・逆算の一致・手取り率の範囲・控除の境界
- 【B】費目と暮らし方 … 表示の出し分け、月割り、**生徒の入力が守られるか**
- 【C】画面と操作 … 7画面が描けるか、＋−と直接入力、賞与の下限
- 【D】求人票と判定 … 計算式、通勤手当の上限、**クイズの正解が計算値と一致するか**、判定のしきい値
- 【E】サンドボックス耐性 … confirm/alert 不使用、try で包んだ保存、外部読み込みゼロ

> **クイズの正解は、求人データから計算した値と突き合わせて検査している。**
> 求人の金額を変えても、選択肢の文言と正解が自動で追従する。手で直す必要はない。

### 画面幅の確認（手作業）

Chromium で 390 / 768 / 1366 px の3つを開き、**横スクロールが出ないこと**を見る。
広い表には `class="plain wide"` を付けて、その表だけが横に流れるようにしてある。
`.cols>*{min-width:0}` を消すと、表が列を押し広げて画面から溢れる。消さないこと。

---

## 7. 意図的に実装していないもの

- **提出機能**（ユーザーと確定。授業内で考えさせる道具にとどめる）
- 進学ルート（学費・奨学金の返済）。就職希望が中心という前提で作ってある
- 実在求人の取り込み。求人票は架空。**実物は進路指導室にある**
- 扶養・配偶者控除・生命保険料控除など。18歳独身には効かない
- 手取りの月ごとの変動（賞与月・6月の住民税切り替え）。年額を12で割った平均で見せている

---

## 8. 授業での使い方（案・未実施）

1コマ目: STEP1〜4。自分の必要手取りを出すところまで。
2コマ目: STEP5〜7。求人票を読み、最後に**進路指導室の本物の求人票**を1枚ずつ配って同じ欄をたどる。

STEP6の「暮らし方を変えたら」の表を、**教室の前で全員分並べる**と効く。
同じ求人でも、実家か一人暮らしかで判定が変わるのが一目で分かる。

---

## 9. 次にやると効きそうなこと（優先度順・ユーザー未確定）

1. **昇給を10年分伸ばす画面**。昇給しても住民税と社会保険料が一緒に増える様子
2. 求人票に**わざと欠けのある1件**を足す（固定残業の時間数が空欄、加入保険が2つだけ）。
   「質問すべき求人」を見抜かせる
3. 地域の最低賃金を入力させ、`jikyu` と比べる
4. 車を「持たない年数」を選ばせ、貯金がいくら積み上がるかを見せる
