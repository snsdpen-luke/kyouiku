# 複線図れんしゅう — 引き継ぎ書

第二種電気工事士 技能試験の候補問題を、**単線図から複線図に描き直す**練習アプリ。
生徒が Chromebook で開き、単線図を読んで**部品を置き**、タップで線を引き、スイッチを操作して確かめ、判定する。

## 0. 画面（2026-09-24d〜）

| 場所 | 中身 |
|---|---|
| 左（大） | 複線図を描く所。最初は空っぽ |
| 右上 | **単線図**。問題データのお手本の配置から自動で描く（公式問題も同じ）。タップで拡大 |
| 右中 | **部品図**。いつも同じ一覧（電源・ジョイントボックス・ランプレセプタクル・引掛シーリング・スイッチ・3路スイッチ・コンセント・パイロットランプ・換気扇）。どれが要るかは生徒が単線図から決める |
| 右下 | 判定の結果とヒント、描き方（折りたたみ） |
| 上の帯 | 線の色、スイッチ操作、判定・ヒント・お手本・消す |

### 部品図は公表問題の図記号（2026-09-24h・ユーザー要望）
令和8年度の公表問題（No.1〜13）の配線図にある図記号に合わせて作り直した（`sym()`。JIS C 0303 準拠）。
見出しで切り替える（`PALETTE`）。

| 見出し | 器具 |
|---|---|
| ボックス・電源 | ジョイントボックス（斜線3本の円）・アウトレットボックス（四角）・電源・配線用遮断器 B・漏電遮断器 BE・接地極 E_D・電動機 M |
| 照明 | ランプレセプタクル Ⓡ・引掛シーリング（角）・引掛シーリング（丸）・蛍光灯・屋外灯・確認表示灯 ○ |
| 点滅器 | スイッチ ●・3路 ●3・4路 ●4・位置表示灯内蔵 ●H・自動点滅器 ●A(3A)・タイムスイッチ TS・リモコンリレー ▶ |
| コンセント | コンセント・2口・接地極付 E・接地端子付 ET・接地極付接地端子付 EET・20A 250V E・露出形 |

- ずっと施工省略の器具（リモコンスイッチ ●R・リモコントランス Ⓣ・接地端子）は入れていない。換気扇は公表問題に無いので部品図から外した（型は練習用に残す）
- 電源の仲間は `source:true`、ボックスの仲間は `box:true`（`isSrc()`・`isBox()`）。種類名を直接比べない
- 問題データのボックスは `kind: "obox"` でアウトレットボックスになる。ジョイントボックスとアウトレットボックスは取り違えると対応しない
- 接地の端子 E は緑の札。電位 `E` は 0V

**要確認（公式問題を入れるときに実物・施工条件で確かめる）**
- 4路スイッチの中のつながり: 平行 1–2・3–4 ／ 交差 1–4・3–2 と仮に置いた
- No.13 の施工省略の器具（円の中に切れ目のある円）を「屋外灯」とした
- 自動点滅器（1 電源・2 接地側・3 負荷）とタイムスイッチ（S1 電源・S2 接地側・L1 負荷）の端子の役目は、試験の施工条件（端子台で代用）に合わせて確かめる
- 電動機の端子は U・V・W（W は白の札ではなく普通の文字）

### 部品を置く流れ
1. 部品図をタップ → 図の置きたい所をタップ → 置いた部品が選ばれた状態になる
2. 上の帯で**記号**を選ぶ（イ・ロ・ハ…／なし。ボックスは B1〜B4。電源は記号なし）
3. 種類と記号が単線図と同じなら、その部品は問題の器具に**対応**する（`S.placed[].pid`）。
   同じ種類・記号が複数あるとき（3路スイッチ イ が2つなど）は、お手本の配置で一番近いものに対応させる
4. 対応した部品どうしのあいだにだけ、**ケーブルの帯（薄く）と「VVF1.6-2C」の表示**が出る。
   記号を間違えると帯が出ない＝読み違いに自分で気づける
5. 対応しない部品（余分・記号ちがい）も置ける。**置いた時から端子（W/B の札・スイッチの2点など）は出す**（2026-09-24e・ユーザー要望）。端子の向きは一番近いボックスへ。ただし線は引けない（タップすると「記号を合わせると線を引ける」と伝える。`tx:` の当たり判定）。判定で「部品」として指摘する
- 選んだ線・●・部品の操作の帯は、図の上に重ねて出す（図が押し下げられて下が切れないように）
- 部品・ボックスはドラッグで動かせる。ボックスを動かすと、動かした●も一緒に動く
- 部品を消す・記号を変えると、その部品につながっていた線は消える（ボックスなら中の●と線も）
- 「消す」は「線だけ消す」と「部品も全部片づける」を選べる
- 部品を置く前の版（〜24c）で保存した図は、お手本の配置で部品を置いた状態で開く

### 端子の札
W・N は白地、B・L は黒地、L2 は赤地の札で出す（つなぐ線の色）。札は端子の並びの外側に置き、器具の記号にかぶせない。

- 本体: `複線図練習.html`（単一HTML・依存なし・通信なし）
- 配布物: `fukusen/index.html`（`build.py` の生成物。手で編集しない）
- 検証: `node regression-fukusen.js 複線図練習.html`

## 1. 設計の要

### 線は「ケーブルの中」か「同じ枠の中」でしか引けない
- 線（心線）の端は **器具の端子** か **ボックスの中の接続点（●）**
- 端子 → ボックス をタップすると、その間の**ケーブル**を通る線になる。ケーブルが無い所へは引けない
- 同じ `place`（連用取付枠など）の端子どうしは**渡り線**として直接引ける
- 同じボックスの接続点どうしはつながない（1つの接続点にまとめさせる）

見た目（線の並び）は生徒ごとに違うので、**見た目では判定しない**。

### 線はドラッグでも引ける（2026-09-24f・ユーザー要望）
- **端子（○）からドラッグ** → 指まで点線が伸びる → 端子・●・ボックスの上で離すとつながる。何も無い所で離すと取りやめ
- 伸ばしている間は、つなげるボックスを青く、指の下の端子・●に青い輪を出す（離せばつながる所）
- **ボックスの円の中で離せば、下に線が通っていてもボックスで離したことにする**（線の当たり判定に吸われて取りやめになる実害があった）
- 離した所の判定は `hitAt()`（`elementFromPoint`）。伸ばしている線は `pointer-events="none"`
- **●からのドラッグ**（2026-09-24g・ユーザー要望）: ボックスの中で動かしている間は●が動く。**ボックスの円の外まで引っぱり出すと線を伸ばすに切り替わり**、●は元の位置に戻る。器具の端子・ほかのボックス（の●）で離すとつながる。自分のボックスに戻って離すと取りやめ（エラーにしない）。縁が境目なので、どちらの操作か見て分かる
- タップ → タップで引く方法も残してある
- ●は自動で並び直すので、生徒が「空いている」と思った所に●が来ることがある。離す前の青い輪で見分けられる

### 単線図の枠の器具は積む（2026-09-24o・ユーザー要望）
- 単線図では、同じ枠（place）の器具を、公表問題の図と同じくぴったり積む（●H イ・●ロ・●ハ、●＋コンセント など）
- 積む向きは、ボックスから来るケーブルの向き（縦なら縦、横なら横）。リモコンリレーは縦。電源側のまとまりは積まない
- ボックスに近い順＝データの `parts` の順。**枠の器具はボックスに近い方から書く**（No.2 は スイッチ → パイロットランプ）
- 記号（イ・ロ…）は、縦に積んだら右、横に並べたら上
- **複線図の器具の向きは変えない。** 一度「複線図の器具を縦に立てる」と取り違えて作り、ユーザーに差し戻された（24n を revert）。縦にするのは単線図の話

### 複線図のスイッチは接点の記号（2026-09-24l・ユーザー要望）
- 複線図の上では、スイッチ・位置表示灯内蔵・リモコンリレーを「端子の○2つと刃」で描く（黒い●は描かない。`contact()`）。刃は器具の内側へ倒す
- 確かめるモードで入にすると、刃がもう片方の端子に倒れる
- 3路は端子を 1・0・3 の順にし、0 から 1 か 3 へ刃を向ける。4路は中のつながり（平行／交差）を線で描く
- 部品図と単線図は、公表問題の記号（●）のまま。自動点滅器は複線図では四角い端子台（A）で描く

### PC とスマホ（2026-09-24k・ユーザー要望）
- 980px 以下は1列。単線図 → 部品図 → 操作の帯 → 図 → 結果 の順（`.left`・`.side` を `display: contents` にして `order` で並べる。`.main >` を付けないと後ろの `.side{display:flex}` に負ける）
- 600px 以下: 問題のボタンは1行で横スクロール、ボタンを小さく
- 図の右下に「− ＋ ✋移動 全体」。見える範囲（`vbox`）の縦横比は図の枠に合わせる（スマホの縦長の枠で、拡大すると縦も使える）
- 移動モードでは、どこをつかんでも図が動き、タップで描かない
- 図の上で指を動かしている間は `touchmove` を `preventDefault` する。しないと、動かした直後のボタンのタップがブラウザに捨てられる（実測。約1秒以内のタップ）
- 単線図のケーブルの種類: 縦の線は縦書き、横の線は線の上（両端の記号のあいだ）、斜めの線はボックスから見て外側

### 1つ戻す（2026-09-24i・ユーザー要望）
- 上の帯の「↶ 1つ戻す」か Ctrl+Z。`save()` のたびに、変える前の図（JSON）を `hist` に積む（最大100手）
- 部品を置く・線・色・●を動かす・まとめる・全部片づける、どれも戻せる。ドラッグは離した時に1手
- 問題を切り替えると履歴は空。お手本の表示中は押せない

### あとから直せる（2026-09-24b）
- **線**をタップ → 色を変える／**つなぎ先を変える**（ボックス側の端を別の●へ。ボックスの空いた所なら新しい●）／消す。渡り線は付け替え不可（消して引き直す）
- **●**をタップ → **動かす**（ボックスの中の置きたい所をタップ。縁の外は内側へ寄せる。外すぎると受け付けない）／**まとめる**（同じボックスの●どうし。重複する線は1本にする）／消す（つながる線ごと。自前の確認つき）
- ●を選んだまま別のボックスをタップすれば、ボックス間の線も引ける
- **●はドラッグでも動かせる**（2026-09-24c・ユーザー要望）。Pointer Events でマウスと指の両方に効く
  - 6px 動かすまではタップ扱い（選ぶ操作と取り違えない）。ドラッグの終わりはタップにしない（`dragEnded`）
  - ボックスの外まで引っぱっても縁で止まる。離した位置は保存される
  - 描き直すたびに svg の中身が作り直されるので、`setPointerCapture` は svg 本体にかける
  - 図の上は `touch-action: pinch-zoom`。指でつかんだとき画面がスクロールしない（拡大は残す）
  - タップだけで動かす方法（●をタップ→「動かす」→置きたい所をタップ）も残してある
- 動かした●は `x,y` を持つ。持たない●は従来どおり自動で並ぶ
- **電源の端子の並び**は、電源の下の「上下入れ替え」で逆にできる（`S.flip`。保存される。判定には影響しない）
- 「線をすべて消す」でも入れ替えは残す

### 端子の表示は W / B
ランプレセプタクル・引掛シーリング・コンセント・換気扇の端子は、図では **W（白をつなぐ・接地側）/ B（黒をつなぐ）** と出す。
ランプレセプタクルの W は受金ねじ部、B は中心接触部。メッセージでは「W（受金ねじ部）」のように添える（`desc`）。

### 判定は電気の計算でやる
- 導線・接続点・閉じたスイッチでつながった点を1つの節点にまとめ、負荷を抵抗として節点電圧を解く（複素数。三相にも使える）
- **すべてのスイッチの組み合わせ**で計算し、`expect` と比べる
- 3路スイッチは 1 と 3 の入れ替えでも正しいので、`any:true`（反対の動きでも可）で判定する
- パイロットランプの異時点滅（ランプと直列）も、抵抗の比（ランプ 100Ω / PL 100kΩ）で自然に出る

### 判定の順と中身
| 分類 | 何を見るか |
|---|---|
| 部品 | 単線図の器具・ボックスを置き忘れていないか／単線図と対応しない部品（余分・記号ちがい）がないか |
| 未完成 | つないでいない端子／線が1本だけの接続点／ケーブルの心線数と線の本数が違う（置いた部品についてだけ見る） |
| 短絡 | どれかのスイッチの組み合わせで、電源の線どうしが直接つながる |
| 動作 | スイッチの組み合わせごとの点き方が `expect` と違う／定格より高い電圧 |
| 接地側 | `w:true` の端子（受金ねじ部・W表示）が、スイッチを通さずに電源 N につながっていない |
| 色 | ケーブルの心線の色の組み合わせ（2C は黒白、3C は黒白赤）／電源の N から直接つながる線は全部白、L から直接つながる線は全部黒（`potColor`） |

**白をスイッチの帰り線に使うのは誤りにしない。** 2心ケーブルでスイッチへ行くと、白が帰り線になるのが普通。

### ヒントは答えを先に配らない
ユーザー（先生）の方針。生徒が自分で気づく余地を残す。
1. 判定: 分類と件数だけ
2. ヒント1段目: 場所（図に赤で印）
3. ヒント2段目: 理由（決まり）
お手本は「お手本を見る」から、自前の確認を挟んで出す。自分の図は消えない。

## 2. 問題データの足し方

`PROBLEMS` に1問ずつ足す。公式の候補問題は `official:true, no:番号` を付けると No.1〜No.13 のボタンに入る。

```js
{
  id: "k1", official: true, no: 1, tab: "No.1", title: "No.1　…", note: "…",
  potColor: { L: "黒", N: "白" },                   // 電源から直接つながる線の色
  parts: [
    { id: "PW", type: "src", label: "電源", sub: "1φ2W 100V", x: 110, y: 310,
      terms: [{ k: "L", lab: "L", pot: "L" }, { k: "N", lab: "N", pot: "N" }] },
    { id: "La", type: "lamp", label: "イ", x: 500, y: 80 },
    { id: "Sa", type: "sw1", label: "イ", x: 440, y: 555, place: "SW" },   // place が同じ＝同じ枠
  ],
  boxes: [{ id: "B1", x: 500, y: 310 }],
  cables: [{ a: "PW", b: "B1", spec: "VVF1.6-2C", colors: ["黒", "白"] }, …],  // a/b は place か box
  expect: [{ p: "La", f: s => s.Sa }],               // s はスイッチの状態。3路は any:true
  ref: [["PW.L", "B1:1", "黒"], …],                  // お手本。"器具.端子" か "ボックス:接続点番号"
}
```

- 図の座標は viewBox `0 0 1000 620`。**この x,y が単線図とお手本の配置になる**（生徒の図の配置とは別。`prep()` で `ax,ay` に控える）。公式の単線図の配置に似せて置く
- 器具の種類は `TYPES`。足りない物（4路・自動点滅器・タイムスイッチ・200V コンセント・三相の端子台など）は、`terms`／`el`（抵抗と定格）／`sw`（状態ごとにつながる端子の組）で足す
- 電源の電位は `POT`。単相3線は L1=+100, N=0, L2=−100。三相は R/S/T を 120° ずらしてある
- **足したら必ず `regression-fukusen.js` を回す。** 全問について、お手本が合格・空の図が不合格・典型的な誤答が落ちることを自動で見る

## 3. 公式の候補問題（未着手）

令和8年度の候補問題は 2026-01-16 に電気技術者試験センターが公表している。
作業環境のネットワーク設定で `www.shiken.or.jp` が遮断されていて、まだ読めていない。
**記憶で問題データを作らないこと。** 回路を間違えた練習アプリは害になる。公式の配線図を見ながら入れる。

## 4. 守っている約束（ほかの教材と同じ）

- 単一HTML・ビルド不要・通信なし。Chromebook のブラウザで開く
- タップだけで描ける（ドラッグは●を動かすときの近道。無くても全部できる）
- ブラウザの `confirm` / `alert` を使わない（Classroom のプレビューで死ぬ）。自前の `ask()` を使う
- 画面の文章は普通の説明文（キャラクター口調にしない）
- 画面右上に版を出す。直したら `const VER` を更新する
- 描いた図は `localStorage` に保存。保存できない環境でも動く（合格の印は開いている間だけ残る）
- パソコンがダークモードでも白い画面（`color-scheme: light`）

## 5. 判定の範囲外（意図して入れていないもの）

- リングスリーブの種類と刻印、差込形コネクタの極数（ユーザーが「結線＋電線の色」までと決めた）
- 電線の太さ（1.6 / 2.0）の使い分け
- 施工条件のうち問題ごとの特別な指定（入れるなら `judge()` に問題ごとの追加検査を足す）
